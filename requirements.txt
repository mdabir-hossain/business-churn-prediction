# Core runtime dependencies for the current notebook/project.
# Version ranges are intentionally moderate because the original environment
# was not captured in the audit.

numpy>=1.24,<3.0
pandas>=2.0,<3.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<0.14
scikit-learn>=1.3,<2.0
shap>=0.44,<1.0
PyYAML>=6.0,<7.0
jupyterlab>=4.0,<5.0
ipykernel>=6.0,<7.0
